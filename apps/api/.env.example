# Environment is loaded from the MONOREPO ROOT (.env / .env.example) at startup via src/env.ts.
# This file exists so the app has a committed example even if copied out of the monorepo.
MONGODB_URI=mongodb://localhost:27017/nexteduwise
PORT=3000